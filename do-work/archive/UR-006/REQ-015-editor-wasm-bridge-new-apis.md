---
id: REQ-015
title: "Update editor WASM bridge for new goTTP APIs"
status: completed
created_at: 2026-02-13T22:00:00Z
claimed_at: 2026-02-13T22:20:00Z
completed_at: 2026-02-13T22:50:00Z
route: C
user_request: UR-006
related: [REQ-014, REQ-016]
batch: editor-update
---

# Update Editor WASM Bridge for New goTTP APIs

## What
Update `editor/wasm/main.go` to expose the new goTTP APIs through the WASM bridge so JavaScript can use them: ParseOptions.Lookups, ParseOptions.Vars, ParseOptions.Functions, CompileTemplateWithOptions, and CompileOptions.Functions.

## Detailed Requirements

- Expose `ParseOptions.Lookups` — allow JavaScript to pass lookup tables when parsing
- Expose `ParseOptions.Vars` — allow JavaScript to pass dynamic variables when parsing
- Expose `CompileTemplateWithOptions` — allow JavaScript to compile with CompileOptions
- Assess feasibility of custom functions (Go closures) in WASM context:
  - Custom functions are Go closures — can JavaScript provide these via WASM?
  - If not directly possible, document the limitation
  - Consider alternatives: predefined function sets, Starlark macros via template, etc.
- Update the `parseTemplate()` WASM function to accept lookups and vars parameters
- Assess whether goTTP loader helpers (`LoadLookupFromJSON`, `LoadLookupFromYAML`, `LoadLookupFromCSV` from REQ-011) can be exposed through WASM for in-browser lookup table loading from user-uploaded files
- Maintain backward compatibility — existing calls without new parameters still work

## Builder Guidance

- Certainty level: Exploratory — the custom functions aspect may have WASM limitations
- The WASM bridge currently exports 7 functions via `syscall/js` — new functionality may add new exports or extend existing ones
- Key file: `editor/wasm/main.go`
- Key reference: `editor/js/wasm-bridge.js` (JavaScript side of the bridge)
- Lookups/vars should be straightforward (JSON data). Custom functions may not be feasible in WASM — assess and document.

## Dependencies

- Depends on REQ-014 (WASM binary must be rebuilt with new goTTP first)

---
*Source: See UR-006/input.md for full verbatim input*

---

## Triage

**Route: C** - Complex

**Reasoning:** Multi-file changes (Go WASM bridge + JS bridge), custom functions feasibility assessment, API design for passing complex data types between JavaScript and Go via WASM, backward compatibility requirements. Needs planning to determine what's feasible and how to wire it.

**Planning:** Required

## Plan

### Feasibility Assessment

| Feature | Feasible? | Notes |
|---------|-----------|-------|
| ParseOptions.Lookups | Yes | JSON data, add 7th arg to parseTemplate |
| ParseOptions.Vars | Already works | varsJSON already passed as Parse() vars param |
| CompileTemplateWithOptions | Partial | New function, but Functions requires pre-registered Go sets (not JS callbacks) |
| Custom Functions from JS | No | Go closures can't be created from JavaScript at runtime via WASM |
| Loader helpers | Yes | Pure data transforms, all deps already in WASM binary |

### Custom Functions Limitation

Go WASM's `syscall/js` allows Go→JS calls but NOT JS→Go closure creation. Custom functions (MatchFunc, GroupFunc, etc.) are Go function signatures — JavaScript cannot create Go closures at runtime. **Workaround:** Pre-registered Go function sets referenced by name. **Alternative:** Users should use `<macro language="starlark">` or `<macro language="javascript">` tags for custom logic.

### Implementation Steps

1. **parseTemplate()**: Add optional 7th arg `lookupsJSON`, update ParseOptions construction to include Lookups
2. **Loader helpers**: Add `loadLookupFromJSON`, `loadLookupFromYAML`, `loadLookupFromCSV`, `loadLookupsFromJSON` WASM functions
3. **compileTemplateWithOptions**: Add new WASM function with function set registry pattern + `listFunctionSets`
4. **wasm-bridge.js**: Add corresponding JS methods, update parseTemplate signature
5. **Rebuild WASM**: `make build`

### Files to Modify
- `editor/wasm/main.go` — all Go-side changes
- `editor/js/wasm-bridge.js` — all JS-side changes

*Generated by Plan agent*

## Exploration

### editor/wasm/main.go (406 lines)
- Imports: `gottp`, `gottp/internal/formatters`, `encoding/json`, `syscall/js`, `sync`
- `compileTemplate()`: lines 19-77, takes template text string, uses `gottp.CompileTemplate()`
- `parseTemplate()`: lines 81-217, takes 3+ args (compiledTemplateJSON, inputsJSON, varsJSON, [yangModulesJSON], [cacheKey], [enableSourceMap])
- **ParseOptions construction**: lines 161-168, only sets YANGModules and EnableSourceMap currently
- `main()`: lines 391-405, exports 7 functions to `js.Global().Set("gottp", ...)`
- Template cache: `compiledTemplateCache` map with `cacheMutex`

### editor/js/wasm-bridge.js
- `GottpWasmBridge` class, constructor lines 7-12
- `compileTemplate()`: lines 104-141, calls `this.gottp.compileTemplate(templateText)`
- `parseTemplate()`: lines 153-207, signature: `async parseTemplate(compiledTemplate, inputsJSON, varsJSON=null, yangModulesJSON=null, cacheKey=null, enableSourceMap=false)`
- WASM call with 6 positional params at lines 177-184
- Global instance: line 352, `const wasmBridge = new GottpWasmBridge()`

*Generated by Explore agent*

## Testing

**Tests run:** `go test ./...`
**Result:** All tests pass (0 failures)
**Note:** WASM bridge functions are browser-only — no automated WASM tests. Go library tests confirm underlying APIs (LoadLookupFromJSON, etc.) work correctly. Manual browser testing recommended.

*Verified by work action*

## Implementation Summary

### Files Modified

| File | Changes |
|------|---------|
| `editor/wasm/main.go` | Added lookups support to `parseTemplate()` (7th arg), added 4 lookup loader functions (`loadLookupFromJSON`, `loadLookupFromYAML`, `loadLookupFromCSV`, `loadLookupsFromJSON`), added `compileTemplateWithOptions` with function set registry, added `listFunctionSets`, registered all in `main()` |
| `editor/js/wasm-bridge.js` | Updated `parseTemplate` with `lookupsJSON` param, added 4 lookup loader methods, added `compileTemplateWithOptions` and `listFunctionSets` methods |
| `editor/gottp.wasm` | Rebuilt with all new WASM exports |

### New WASM Functions (7 total)

| Go Function | JS Method | Purpose |
|-------------|-----------|---------|
| `loadLookupFromJSON` | `loadLookupFromJSON(name, jsonData)` | Parse JSON into lookup table |
| `loadLookupFromYAML` | `loadLookupFromYAML(name, yamlData)` | Parse YAML into lookup table |
| `loadLookupFromCSV` | `loadLookupFromCSV(name, csvData, keyColumn)` | Parse CSV into lookup table |
| `loadLookupsFromJSON` | `loadLookupsFromJSON(jsonData)` | Parse multi-table JSON |
| `compileTemplateWithOptions` | `compileTemplateWithOptions(text, options)` | Compile with function set |
| `listFunctionSets` | `listFunctionSets()` | List registered function sets |
| (modified) `parseTemplate` | `parseTemplate(..., lookupsJSON)` | Added 7th arg for lookups |

### Custom Functions Assessment

Custom functions from JavaScript are **not feasible** — Go closures cannot be created from JS at runtime via WASM's `syscall/js`. Workaround: pre-registered Go function sets referenced by name via `compileTemplateWithOptions({functionSet: "name"})`. Alternative: users should use `<macro language="starlark">` or `<macro language="javascript">` tags for custom logic.

*Completed by work action (Route C)*
