---
id: REQ-016
title: "Update editor UI for lookups, vars, and custom functions"
status: completed
created_at: 2026-02-13T22:00:00Z
claimed_at: 2026-02-13T22:55:00Z
completed_at: 2026-02-13T23:20:00Z
route: C
user_request: UR-006
related: [REQ-014, REQ-015]
batch: editor-update
---

# Update Editor UI for Lookups, Vars, and Custom Functions

## What
Add UI panels and controls to the web editor so users can manage lookup tables, dynamic variables, and (if feasible) custom functions through the browser interface.

## Detailed Requirements

- **Lookup tables**: The editor already has a "Lookup Tables" UI panel — determine whether it wires through to the new `ParseOptions.Lookups` field or uses an older mechanism, and update accordingly rather than building from scratch
  - Support JSON format for lookup table input
  - Support loading lookup tables from files (JSON, YAML, CSV via loader helpers if available in WASM)
- **Dynamic variables**: The editor already has a "Global Variables" UI panel — determine whether it wires through to the new `ParseOptions.Vars` field or uses an older mechanism, and update accordingly rather than building from scratch
- **Custom functions** (if feasible per REQ-015 assessment):
  - If WASM can support JS→Go function callbacks, add UI for defining custom functions
  - If not, document limitation and consider alternative approaches
- All new UI should match the existing editor's dark theme and design patterns
- New features should be accessible from the existing menu/panel system

## Builder Guidance

- Certainty level: Exploratory — scope depends on REQ-015's feasibility findings for custom functions
- The editor already has a "Global Variables" feature and "Lookup Tables" feature — need to assess how these interact with the new goTTP ParseOptions fields
- Key files: `editor/js/app.js` (main UI logic, 79KB), `editor/index.html`, `editor/css/main.css`
- Scope cue from user: "see what it would take" — this is investigative, assess what's possible/useful

## Dependencies

- Depends on REQ-015 (WASM bridge must expose new APIs first)
- Depends on REQ-014 (WASM binary must be rebuilt first)

---
*Source: See UR-006/input.md for full verbatim input*

---

## Triage

**Route: C** - Complex

**Reasoning:** Large existing codebase (app.js ~79KB), need to assess existing lookup/vars UI wiring vs new ParseOptions fields, multi-file changes across HTML/JS/CSS, custom functions UI assessment based on REQ-015 findings.

**Planning:** Required

## Plan

### Current State Assessment

| Feature | Status | Details |
|---------|--------|---------|
| Variables UI | Working | `state.variables` → serialized as varsJSON → passed as 3rd arg to parseTemplate |
| Lookups UI | Stub only | Modal HTML exists, CSS prepared, state scaffolded, but `showLookupsModal()` is a stub that shows "coming soon" |
| Lookups in parseTemplate | Not wired | `process()` only passes 6 args — missing 7th `lookupsJSON` arg |
| Custom Functions UI | Not needed | WASM can't create Go closures from JS, registry starts empty |

### Implementation Steps

1. **Wire lookups into parseTemplate call**: In `process()`, serialize `state.lookups` as JSON and pass as 7th arg — highest impact, lowest effort
2. **Expand lookups modal HTML**: Replace minimal modal in index.html with file upload, paste content, format select, CSV key column, loaded lookups list
3. **Implement lookups JS logic**: Replace `showLookupsModal()` stub, add `setupLookupHandlers()`, `loadLookupFromFile()`, `loadLookupFromPaste()`, `updateLookupsList()`, `removeLookup()`
4. **File loading**: Smart format detection by extension (.json → try multi-table then single-table, .yaml/.yml, .csv)
5. **Custom functions**: No action — document WASM limitation in code comment
6. **Optional**: Config menu badge indicators for loaded lookups/vars count

### Files to Modify
- `editor/js/app.js` — main logic (~150 new lines, ~5 modified)
- `editor/index.html` — expand lookups modal
- `editor/css/main.css` — minimal if any (styles already exist)

*Generated by Plan agent*

## Exploration

Performed during planning — Plan agent explored app.js process() method, showLookupsModal() stub, init(), YANG module handlers pattern, index.html lookups modal, and CSS lookup-item styles.

*Included in Plan phase*

## Testing

**Tests run:** `go test ./...`
**Result:** All tests pass (0 failures)
**Note:** UI changes are browser-only — manual testing recommended (file upload, paste, remove, process with lookups)

*Verified by work action*

## Implementation Summary

### Files Modified

| File | Changes |
|------|---------|
| `editor/js/app.js` | Wired `state.lookups` as 7th arg to `parseTemplate` in `process()`; replaced `showLookupsModal()` stub; added `setupLookupHandlers()`, `loadLookupFromFile()`, `loadLookupFromPaste()`, `updateLookupsList()`, `removeLookup()`; added custom functions WASM limitation comment |
| `editor/index.html` | Replaced minimal lookups modal with full-featured modal: file upload (.json/.yaml/.yml/.csv), paste content form with format select and CSV key column, loaded lookups list |
| `editor/gottp.wasm` | Rebuilt with latest changes |

### What Works Now

- Lookups wired end-to-end: load from file/paste → store in state → pass to parseTemplate via WASM → available in template processing
- File upload supports JSON (single/multi-table auto-detect), YAML, CSV
- Paste supports same formats with manual name and format selection
- Loaded lookups persist across sessions via localStorage
- Lookups included in workspace export/import
- Custom functions UI intentionally omitted (documented WASM limitation)

*Completed by work action (Route C)*
