---
id: REQ-011
title: "Lookup loader helpers"
status: completed
created_at: 2026-02-13T19:12:17Z
claimed_at: 2026-02-13T19:48:00Z
route: B
completed_at: 2026-02-13T20:10:00Z
user_request: UR-004
related: [REQ-010]
batch: dynamic-injection
---

# Lookup Loader Helpers

## What
Provide convenience functions to load lookup tables from common file formats (JSON, CSV, YAML), returning maps that can be passed directly to `ParseOptions.Lookups`.

## Detailed Requirements

- `LoadLookupFromJSON(name string, data []byte) (map[string]map[string]interface{}, error)` — parse JSON into a named lookup table
- `LoadLookupFromCSV(name string, data []byte) (map[string]map[string]interface{}, error)` — parse CSV into a named lookup table (first row as headers, key column configurable)
- `LoadLookupFromYAML(name string, data []byte) (map[string]map[string]interface{}, error)` — parse YAML into a named lookup table
- Each function returns a single-entry map (`name -> table`) that can be merged into `ParseOptions.Lookups`
- Match Python TTP's loader patterns for feature parity where applicable
- Handle common error cases: malformed input, empty data, missing key column (CSV)

## Constraints

- These are convenience functions — raw `map[string]map[string]interface{}` must also work without loaders
- Depends on REQ-010 (the ParseOptions.Lookups field must exist for these to be useful)
- User wants both raw maps AND loader helpers, not one or the other
- User is consuming this from another project — API ergonomics matter

## Dependencies

- Depends on: REQ-010 (runtime injection mechanism) — loaders produce data that feeds into `ParseOptions.Lookups`

## Builder Guidance

- Certainty level: Firm — user explicitly requested "support both raw maps and loader helpers"
- These are public API functions, likely in the `gottp` package or a `gottp/lookup` sub-package
- CSV loader may need a key column option (e.g., first column as lookup key by default)
- Consider whether file-path variants (LoadLookupFromJSONFile, etc.) are needed or if []byte is sufficient

## Open Questions

- Input format: Should loaders accept `[]byte`, file paths (`string`), `io.Reader`, or all three? The Q&A confirmed JSON/CSV/YAML formats but didn't specify the Go input mechanism. Builder should decide based on API ergonomics.

## Full Context
See [user-requests/UR-004/input.md](./user-requests/UR-004/input.md) for complete verbatim input and design Q&A.

---
*Source: See UR-004/input.md for full verbatim input*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear feature with well-defined signatures (3 loader functions). Need to explore existing YAML/CSV loading patterns in the codebase and the `gottp` package layout to determine best placement.

**Planning:** Not required

## Plan

**Planning not required** - Route B: Exploration-guided implementation

Rationale: Well-defined utility functions with clear input/output types. Just need to find existing loading patterns and package placement.

*Skipped by work action*

## Exploration

- YAML: `gopkg.in/yaml.v3` already in go.mod. JSON/CSV: Go stdlib.
- `internal/variable/loader.go` already has `LoadYAML()`, `LoadJSON()`, `LoadCSV()` methods — can reuse parsing logic
- `internal/lookup/database_loader.go` already exposes public `LoadDatabaseLookup()` — establishes pattern for public loader functions in that package
- **However**, `internal/lookup/` is an internal package — cannot be imported by external consumers. Functions need to go in the main `gottp` package.
- Lookup data type at runtime: `map[string]interface{}` (values are also maps for structured lookups)
- REQ-010 added `ParseOptions.Lookups` as `map[string]map[string]interface{}`  — loaders should return compatible type
- CSV needs a key column parameter (first column default)
- Existing naming: `LoadCompiledTemplate()`, `LoadCompiledTemplateFromBytes()` — suggests `LoadLookupFromJSON()` etc.

*Generated by Explore agent*

## Implementation Summary

- Created `lookup_loaders.go` in main `gottp` package with 6 public functions:
  - `LoadLookupFromJSON(name string, data []byte)` — JSON to named lookup table
  - `LoadLookupFromYAML(name string, data []byte)` — YAML to named lookup table
  - `LoadLookupFromCSV(name string, data []byte, keyColumn string)` — CSV to named lookup table
  - `LoadLookupsFromJSON(data []byte)` — multi-table JSON (3-level nesting)
  - `MergeLookups(lookups ...map[string]map[string]interface{})` — merge utility
  - `convertJSONNumbers()` — internal helper for json.Number conversion
- Created `test/lookup_loaders_test.go` with 11 tests including end-to-end integration

*Completed by work action (Route B)*

## Testing

**Tests run:** `go test ./...`
**Result:** All packages PASS, zero failures

**New tests added** (`test/lookup_loaders_test.go`):
- TestLoadLookupFromJSON_Basic — valid JSON parsing and structure
- TestLoadLookupFromJSON_MalformedJSON — error on invalid JSON
- TestLoadLookupFromJSON_EmptyData — error on empty/nil data
- TestLoadLookupFromJSON_WrongStructure — error on array/non-object
- TestLoadLookupFromYAML_Basic — valid YAML parsing
- TestLoadLookupFromYAML_Malformed — error on invalid YAML
- TestLoadLookupFromCSV_Basic — CSV with explicit key column
- TestLoadLookupFromCSV_DefaultKeyColumn — first column default
- TestLoadLookupFromCSV_MissingKeyColumn — error on missing column
- TestLoadLookupFromCSV_EmptyData — error on empty/nil data
- TestLoadLookup_IntegrationWithParseOptions — end-to-end with compiled template

**Existing tests verified:** All 14+ test packages pass unchanged

*Verified by work action*
