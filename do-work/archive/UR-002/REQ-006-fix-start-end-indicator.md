---
id: REQ-006
title: "Fix TestStartEndIndicatorTogether failure"
status: completed
claimed_at: 2026-02-12T18:31:00Z
completed_at: 2026-02-12T18:45:00Z
route: B
created_at: 2026-02-12T17:32:54Z
user_request: UR-002
related: [REQ-002, REQ-003, REQ-004, REQ-005]
batch: test-failures-and-inner-group
---

# Fix TestStartEndIndicatorTogether failure

## What
The combined start/end indicator test fails because the parser produces an empty result (`{}`) instead of the expected structure containing a `ptp_peers` key. Test `TestStartEndIndicatorTogether` in `test/start_end_indicator_test.go:250,269` shows the result is an empty object.

## Context
From test suite run: when both `_start_` and `_end_` indicators are used together in a template, the parser fails to extract any data. The expected output contains a `ptp_peers` structure but the actual output is `{}`.

---
*Source: Existing test failure from test suite run (REQ-001)*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear failure but need to explore how _start_ and _end_ indicators interact in the runtime.

**Planning:** Not required

## Plan

**Planning not required** - Route B: Exploration-guided implementation

Rationale: Clear expected behavior from test, but need to understand the _start_/_end_ interaction in the compiled runtime.

*Skipped by work action*

## Exploration

- Root cause: `{{ _end_ }}` on its own line compiled to regex `^.*$` which matched every line, causing _end_ to fire on the same line as _start_
- Python TTP uses `\n[\t ]*(?=\n|\r\n)` for standalone _end_, which only matches empty/whitespace lines
- Go equivalent: `^[\t ]*$`

*Generated by Explore agent (via implementation agent)*

## Implementation Summary

- Fixed `internal/pattern/engine.go`: standalone `{{ _start_ }}` and `{{ _end_ }}` patterns now compile to `^[\t ]*$` instead of `^.*$`, matching Python TTP behavior
- Fixed `internal/compiled/runtime.go`: updated `hasEmptyStartPattern` detection to recognize new regex pattern
- Bare `_start_`/`_end_` (without curly braces) retain `^.*$` for backward compatibility

*Completed by work action (Route B)*

## Testing

**Tests run:** `go test ./...`
**Result:** All tests passing (116 in test/, all comparison tests)

**Existing test fixed:**
- `test/start_end_indicator_test.go::TestStartEndIndicatorTogether` - now PASS

**Existing tests verified:** TestStartIndicatorBasic, TestStartIndicatorWithVariable, TestTemplateStartEndIndicator — all still passing

*Verified by work action*
